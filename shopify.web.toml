name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"
[commands]
predev = "npm exec prisma generate -- --schema prisma/dev/schema.prisma"
dev = "npm exec prisma migrate deploy -- --schema prisma/dev/schema.prisma && npm exec react-router dev"
